#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../git-deploy/deploy" "$@"
  ret=$?
else 
  bash  "$basedir/../git-deploy/deploy" "$@"
  ret=$?
fi
exit $ret
